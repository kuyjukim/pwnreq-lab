# pwnreq-lab — security research reproduction lab

Deliberately-vulnerable GitHub Actions `pull_request_target` workflow, used to
confirm a CI pwn-request mechanism **on a repo I own, with a DUMMY secret only**.
No real credentials. See the workflow at `.github/workflows/vulnerable-ci.yml`.
